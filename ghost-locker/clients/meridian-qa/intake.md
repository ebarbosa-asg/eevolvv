# INTAKE — meridian-qa
> Phase 2 · Auto-analysis · Completed 2026-04-12

---

## Build Plan

### Agent Inventory

| # | Agent Name | Pattern | Complexity | Priority |
|---|------------|---------|-----------|----------|
| 1 | QA Defect Reporter | Simple tool use | Low | P0 |

Single-agent engagement. No multi-agent coordination needed.

**Pattern rationale:** Task is deterministic and high-volume. Same steps every night:
fetch data → compute stats → generate report → send email. ReAct loop adds latency with no benefit. Simple tool use is the right call.

---

### Wave Plan

| Wave | Agents | Dependencies |
|------|--------|-------------|
| W1 | QA Defect Reporter | None |

Single wave. No sequencing needed.

---

### Data Sources

```
→ TABLE     ↳ defect_logs (production_db)
→ SCHEMA    ↳ id, line_id, shift_id, defect_code, severity, inspector_id, timestamp
→ VOLUME    ↳ ~400–800 rows/night across all lines
→ ACCESS    ↳ Read-only PostgreSQL, connection string in .env
```

### Data Gaps

- `severity` column uses 1–5 scale but meaning not documented → confirmed with Rachel: 1=minor, 5=critical
- `line_id` references a `production_lines` table — must JOIN to get human-readable line names
- Some nights have zero defects for a line — agent must handle this (not error)

---

### Output Specification

**Report format:** Markdown → Resend renders as HTML email  
**Per-report contents:**
1. Date + run timestamp
2. Summary table: Line | Total Defects | Critical | 3-Sigma Flag | vs. 7-Day Avg
3. Defect breakdown by code (top 5 per line)
4. Flagged outliers section (if any)
5. Footer: "Generated by eevolvv · QA Defect Reporter"

**Email delivery:**
- To: shift supervisors distribution list (3 addresses)
- Subject: `QA Report — {date} — {facility_name}`
- From: reports@eevolvv.com

---

### Complexity Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Data complexity | 2/5 | Clean relational schema, read-only |
| Logic complexity | 2/5 | Stats are straightforward (mean, stddev) |
| Integration complexity | 1/5 | One DB + one email API |
| Output complexity | 2/5 | Structured report, consistent format |
| **Overall** | **2/5** | Low — build in one session |

---

### Build Estimate

- System prompt: 2 hrs
- Tool definitions: 1 hr
- Test data snapshot: 1 hr
- Eval suite: 2 hrs
- **Total: ~6 hrs**

---

**Gate status:** INTAKE ✓ COMPLETE
