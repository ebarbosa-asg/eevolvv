# Brief: IVW-010 — Dental chat intake + INDUSTRY_CONTEXT entry

## Context

Dental visitors need the same depth of vertical-specific treatment that fitness got in IVW-004 and IVW-005: custom intake questions that ask about recall rate, no-show rate, and insurance mix; and a rich INDUSTRY_CONTEXT block that teaches Claude dental vocabulary, real benchmarks, dollar formulas, and which specific automations eevolvv will build. This task layers the dental vertical into both the chat route and the diagnostic prompt.

## Codebase Reality

**`app/api/chat/route.ts`** — after IVW-004, this file has:
- `import { fitnessConfig } from '@/lib/industries'` at the top
- A `buildSystemPrompt(defaultIndustry?: string)` function with a fitness conditional and a generic fallback
- The POST handler calls `buildSystemPrompt(defaultIndustry)`

**Current `buildSystemPrompt` structure** (after IVW-004):
```ts
function buildSystemPrompt(defaultIndustry?: string): string {
  if (!defaultIndustry) return CHAT_SYSTEM_PROMPT

  if (defaultIndustry === fitnessConfig.key) {
    // ...fitness question list...
    return `${CHAT_SYSTEM_PROMPT}\n\nINDUSTRY OVERRIDE: ...\n\n${questionList}\n\n...`
  }

  // Generic fallback
  return `${CHAT_SYSTEM_PROMPT}\n\nINDUSTRY OVERRIDE: ...generic...`
}
```

**`lib/diagnosticPrompts.ts`** — after IVW-001 and IVW-005:
- `'Dental / Oral Health'` entry added (IVW-001) — basic entry with Dentrix terminology and benchmarks
- `'Fitness / Gym / Studio'` entry added (IVW-005) — full expert entry with formulas and 5 automations

IVW-001 created a foundational `'Dental / Oral Health'` entry. This task EXPANDS/REPLACES that entry with the full expert version — same key, richer content. Read the current dental entry first to see what's already there, then expand it.

**`dentalConfig`** (from IVW-009): `key = 'Dental / Oral Health'`, intakeQuestions (7), benchmarks, automationCatalog (5), emailVocabulary.

## What to Build

### Change 1 — `app/api/chat/route.ts`

1. **Update import** to include `dentalConfig`:
   ```ts
   import { fitnessConfig, dentalConfig } from '@/lib/industries'
   ```

2. **Add dental conditional** to `buildSystemPrompt`, after the fitness block:
   ```ts
   if (defaultIndustry === dentalConfig.key) {
     const questionList = dentalConfig.intakeQuestions
       .map((q, i) => `${i + 1}. ${q}`)
       .join('\n')
     return `${CHAT_SYSTEM_PROMPT}\n\nINDUSTRY OVERRIDE: This user came from the Dental / Oral Health landing page. Their industry is already confirmed: "${dentalConfig.key}". Do NOT ask what kind of business they run — skip that question entirely.\n\nAsk their name and practice name first, then work through these dental-specific questions (one or two at a time, in a natural conversational order):\n\n${questionList}\n\nOnce you have their name, practice name, answers to at least 4 of these questions, and their email address, end your message with exactly: [READY] on its own line.`
   }
   ```

The order of conditionals: fitness check first (unchanged), dental check second, generic fallback last.

### Change 2 — `lib/diagnosticPrompts.ts`

**Read the current `'Dental / Oral Health'` entry** (added by IVW-001). Then replace it with the full expert version that matches the quality of the `'Fitness / Gym / Studio'` entry (IVW-005).

The expanded dental entry must include:

**Header:**
`INDUSTRY CONTEXT — DENTAL / ORAL HEALTH:`

**Key automation targets:**
recall campaign outreach (text + email multi-touch), no-show confirmation and same-day rescheduling, insurance pre-authorization tracking and escalation, new patient intake and nurture sequence, treatment plan follow-up and acceptance nudge, AR aging follow-up and payment plan offers, lapsed patient reactivation (12+ months inactive)

**Industry benchmarks (with exact numbers):**
- Recall rate: avg 55–65%, top practices 85–90%; every 10% gain in recall rate = $50K–$100K/yr per provider in recovered production
- No-show rate: avg 4–7%, top practices <1%; each missed slot = $200–$300 in lost production (chair time + overhead)
- Treatment acceptance: avg 55–65%, top practices 70–80%; the gap = scheduled treatment left on the table
- Collection rate: target 98%+; every 1% below 98% = $10K–$50K/yr depending on practice size
- Days in AR: target <30 days; every 30-day band above that costs cash flow and write-off risk
- Dollar-value formula for recall opportunity: `providers × (target_recall_rate - current_recall_rate) / 10 × $75,000 = annual production opportunity`
- Dollar-value formula for no-show cost: `monthly_appointments × no_show_rate × $250_avg_slot = monthly_lost_production`

**Common stack:**
Dentrix, Eaglesoft, Open Dental, Curve Dental, Carestream Dental, Weave (communication), NexHealth (online booking + recalls), Lighthouse 360 (recall + reminders), RevenueWell (marketing automation), Availity (insurance eligibility), Change Healthcare

**Competitive context note:**
Weave and NexHealth are communication and booking platforms — they handle reminders and online scheduling. eevolvv builds automation ON TOP of these: recall campaign intelligence that goes beyond basic reminders, insurance pre-auth tracking when Availity lags, AR follow-up sequences that NexHealth doesn't touch. These tools handle scheduling; eevolvv handles the business outcomes those tools leave on the table.

**Highest-ROI automations (all 5 from the catalog):**
1. Recall Outreach Sequence (Month 1) — multi-touch recall campaign for patients overdue for hygiene; 3-month advance + 1-month + 2-week sequence; industry recall rate lift of 20–30 percentage points is typical
2. No-Show Confirmation & Rescheduling (Month 1) — day-before confirmation with one-tap confirm/cancel; auto-reschedule offer on no-show; recovers 40–60% of would-be no-shows
3. Insurance Pre-Authorization Tracking (Month 2) — tracks pending pre-auth, alerts at 5-business-day threshold, auto-escalates; eliminates treatment delays and billing surprises
4. New Patient Nurture Sequence (Month 2) — 30-day automated welcome: intake form → pre-appointment prep → post-visit check-in → 6-month recall scheduling
5. AR Follow-Up Automation (Month 3) — automated aging AR: 30-day statement, 60-day reminder, 90-day final with payment plan offer; drives Days in AR below 30

**HIPAA note:**
DIAGNOSTIC ONLY: This diagnostic analyzes operational metrics — production, recall rate, AR — not patient records or clinical data. Patient-facing automations that involve PHI (names, DOB, treatment) require a BAA (Business Associate Agreement) before eevolvv accesses production data. Mention this when recommending patient communication automations.

**Language — enforce dental vocabulary:**
Use terms like "patient" (never "customer"), "hygiene appointment" (never "appointment" generically — specify type), "recall rate," "unscheduled treatment," "treatment acceptance," "production" (not "revenue" — dental uses production as the primary metric), "collection rate," "days in AR," "pre-auth," "insurance aging," "recare," "active patient," "new patient goal," "chair time." Do NOT use "client" or "customer."

**Also add** `'Dental / Oral Health': 'Dental'` to the `getIndustryShortName` map if IVW-001 did not add it. Check before adding — avoid duplicate keys.

## Files to Change

| File | Action | Specific Change |
|------|--------|-----------------|
| `app/api/chat/route.ts` | modify | Add `dentalConfig` to import; add dental conditional in `buildSystemPrompt` |
| `lib/diagnosticPrompts.ts` | modify | Expand `'Dental / Oral Health'` INDUSTRY_CONTEXT entry with full expert content |

## Acceptance Criteria

- [ ] `app/api/chat/route.ts` imports `dentalConfig` from `@/lib/industries`
- [ ] A `defaultIndustry === dentalConfig.key` conditional exists in `buildSystemPrompt`
- [ ] When `defaultIndustry === 'Dental / Oral Health'`, the system prompt includes all 7 dental intake questions as a numbered list
- [ ] When `defaultIndustry === 'Dental / Oral Health'`, the prompt says "practice name" (not "business name") in the opener
- [ ] When `defaultIndustry === 'Dental / Oral Health'`, the prompt instructs Claude NOT to ask what kind of business they run
- [ ] Fitness conditional still works unchanged (test: `defaultIndustry === 'Fitness / Gym / Studio'` still returns fitness questions)
- [ ] Generic fallback still works for all other values
- [ ] `INDUSTRY_CONTEXT['Dental / Oral Health']` contains the full expert content (not just the minimal IVW-001 entry)
- [ ] The expanded dental entry contains both dollar-value formulas (recall opportunity, no-show cost)
- [ ] The expanded dental entry contains all 5 automation names from the catalog
- [ ] The expanded dental entry contains the HIPAA note
- [ ] The expanded dental entry instructs Claude to use "patient" not "customer" and "production" not "revenue"
- [ ] The expanded dental entry mentions the competitive context (Weave/NexHealth are communication tools, eevolvv builds on top)
- [ ] `getIndustryShortName('Dental / Oral Health')` returns `'Dental'` (check if IVW-001 added this — do not duplicate)
- [ ] All other INDUSTRY_CONTEXT entries and getIndustryShortName entries are untouched
- [ ] TypeScript compiles without errors (`npx tsc --noEmit`)

## Dependencies

- **IVW-001** — establishes `'Dental / Oral Health'` key in `dental/page.tsx` and creates the initial (basic) entry in `diagnosticPrompts.ts`. This task expands that entry. IVW-001 must be complete first.
- **IVW-004** — establishes the `buildSystemPrompt` function structure in `api/chat/route.ts`. This task adds a second conditional to that same function. IVW-004 must be complete first.
- **IVW-005** — sets the quality bar for what a full expert INDUSTRY_CONTEXT entry looks like. Review the `'Fitness / Gym / Studio'` entry as a template.
- **IVW-009** — provides `dentalConfig` from `lib/industries/dental.ts`. Must be complete before this task can import `dentalConfig`.

## Gotchas

- Read the current `'Dental / Oral Health'` entry in `diagnosticPrompts.ts` before making changes. IVW-001 added a basic entry. This task replaces the value with the expanded version but keeps the key identical. A find-replace on the entry value is sufficient.
- The `buildSystemPrompt` function name is local to `api/chat/route.ts` — it does NOT conflict with the exported `buildSystemPrompt` from `lib/diagnosticPrompts.ts` (which is only imported in `api/diagnostic/route.ts`).
- The `getIndustryShortName` map: check for existing `'Dental / Oral Health': 'Dental'` entry before adding. IVW-001 should have added it. If it's there, do not add it again — duplicate keys in object literals cause TypeScript warnings.
- The HIPAA note in the INDUSTRY_CONTEXT entry is an instruction to the AI model — it tells Claude to recommend a BAA when suggesting patient-facing automations. It does NOT mean the diagnostic itself collects PHI. Be clear in the language about this distinction.
- Dental entry expansion is a string replacement — find the existing `'Dental / Oral Health': \`` line and replace its value with the expanded template literal. All other entries before and after are untouched.
